/**
 * Copyright 2025 Beijing Volcano Engine Technology Co., Ltd. All Rights Reserved.
 * SPDX-license-identifier: BSD-3-Clause
 *
 * =============================================================
 * Node.js 后端服务配置文件：定义后端项目依赖和启动命令
 * =============================================================
 *
 * 【泛化描述】Server/package.json 是 Node.js 后端服务的配置文件。
 *            后端使用 Koa 框架，负责：
 *            - 提供 getScenes 接口（返回场景配置）
 *            - 提供 proxy 接口（代理 RTC API 请求）
 *            - 生成 RTC Token（用于前端加入 RTC 房间）
 *
 * 【字段具体含义 + 典型场景】
 *
 * 【npm scripts】
 *   npm run dev   → 使用 nodemon 启动服务（自动监听文件变化，热更新）
 *   npm run start → 同上（nodemon app.js）
 *
 * 【主要依赖】
 *   @volcengine/openapi  → 火山引擎 OpenAPI SDK（用于签名请求）
 *   koa               → 轻量级 Node.js Web 框架
 *   koa-bodyparser    → Koa 中间件（解析 POST 请求体 JSON）
 *   koa2-cors         → Koa 中间件（处理跨域请求 CORS）
 *   lodash            → JavaScript 工具库（深拷贝、合并对象等）
 *   node-fetch        → Node.js 环境的 fetch API 兼容
 *   uuid              → 生成唯一 ID（用于 RoomId、UserId）
 *
 * 【开发依赖】
 *   nodemon           → 监听文件变化自动重启服务（开发时用）
 *
 * 【典型场景】
 *   cd Server && npm install && npm run dev
 *   → 启动后端服务，监听 3000 端口（或 app.js 中配置的端口）
 */

{
  "name": "AIGCServer",
  "version": "1.0.0",
  "description": "Server for demo to call open api",
  "main": "app.js",
  "license": "BSD-3-Clause",
  "private": true,
  "dependencies": {
    "@volcengine/openapi": "^1.22.0",        // 火山引擎 OpenAPI SDK（AK/SK 签名）
    "koa": "^2.15.3",                       // 轻量级 Web 框架
    "koa-bodyparser": "^4.4.1",              // 解析 POST 请求体（JSON）
    "koa2-cors": "^2.0.6",                  // 处理跨域资源共享（CORS）
    "lodash": "^4.17.21",                   // JavaScript 工具库
    "node-fetch": "^2.3.2",                 // Node.js 环境的 fetch API
    "uuid": "^11.1.0"                        // 生成唯一 ID（RoomId、UserId）
  },
  "devDependencies": {
    "nodemon": "^3.1.10"                    // 自动重启工具（监听文件变化）
  },
  "scripts": {
    "dev": "nodemon app.js",                 // 开发模式（文件变化自动重启）
    "start": "nodemon app.js"                // 生产模式启动
  }
}
