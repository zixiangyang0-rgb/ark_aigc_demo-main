# =============================================================
# Git 忽略文件配置
# =============================================================
#
# 【泛化描述】.gitignore 是 Git 的忽略文件配置，
#             用于告诉 Git 哪些文件/目录不需要纳入版本管理。
#
# 【字段具体含义 + 典型场景】
#
# 【依赖目录】
#   /node_modules        → npm/yarn 安装的依赖包（体积大，不需要提交）
#   .pnp / .pnp.js      → pnpm 的锁文件目录
#
# 【测试和覆盖率】
#   /coverage            → Jest/CI 生成的测试覆盖率报告
#
# 【构建产物】
#   /build              → React 项目的生产构建输出目录
#   dist/               → Python 项目的构建输出目录
#
# 【环境变量文件】
#   .env.local / .env.development.local 等 → 本地环境变量（包含敏感信息）
#
# 【日志文件】
#   npm-debug.log* / yarn-debug.log* 等 → npm/yarn 的调试日志
#   .eslintcache        → ESLint 缓存（加快检查速度）
#   log/                → 应用日志目录
#
# 【Python 相关】
#   **/__pycache__     → Python 字节码缓存目录
#   **/.env            → Python 项目的环境变量文件
#   *.pyc / *.pyo     → Python 编译后的字节码文件
#   **/.venv/         → Python 虚拟环境目录
#
# 【IDE 配置】
#   .vscode/           → VS Code 编辑器配置
#   .idea/             → IntelliJ IDEA 配置
#   *.swp / *.swo     → Vim 编辑器的临时文件
#
# 【操作系统相关】
#   .DS_Store          → macOS 的文件夹元数据文件
#   Thumbs.db          → Windows 的缩略图缓存文件
#
# 【锁文件】
#   yarn.lock          → Yarn 锁文件
#   pnpm-lock.yaml    → pnpm 锁文件
#   **/uv.lock         → uv 包管理器的锁文件

# dependencies
/node_modules
/.pnp
.pnp.js
.vscode

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

/Server/node_modules
/Server/log
/log
yarn.lock
.eslintcache
pnpm-lock.yaml
dist/
log/

**/__pycache__
**/.env
/server_python/scenes/custom.json

# Python
*.pyc
*.pyo
*.egg-info/
*.egg
*.whl

# Virtual environments
**/.venv/
**/venv/

# uv lock files
**/uv.lock

# IDE
.idea/
*.swp
*.swo

# OS
Thumbs.db

# local temp files
.git_commit_msg.txt
